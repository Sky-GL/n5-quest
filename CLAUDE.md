# N5クエスト

JLPT N5の かな・語彙・漢字・文法を、ドット絵RPGの戦闘として覚える学習アプリ。
姉妹版に `Sky-GL/n2-quest`（N2版）がある。作りは同じだが、N5版は前提が違う（後述）。

公開先: https://n5-quest.vercel.app / GitHub: `Sky-GL/n5-quest`

## 動かす

```
node build.mjs          # src/ → n5-quest.html, preview.html, dist/
node serve.mjs          # dist/ を http://localhost:5178 で配る（確認用・gitignore済み）
```

Vercelは `vercel.json` の `buildCommand: node build.mjs` を実行して `dist/` を配る。
`main` へ push すれば自動で本番デプロイされる。`dist/` はgit管理外。

## 構成

```
src/app.template.html   アプリ本体（HTML+CSS+JS）。データ差し込み位置は <script id="__DATA__">
src/kana-a,b.js         ひらがな71 / カタカナ68
src/vocab-a..h.js       語彙805
src/kanji-a,b.js        漢字103字（例語203）
src/grammar-a,b.js      文法88
src/romaji.js           かな→ローマ字の変換器
src/heroes.js           勇者2体（32x32、px=構え / px2=突き）
src/monsters.js,-b.js   敵18体（12x12）
src/icon.js             ファビコンの元絵（16x16）
src/crest.js            タイトルの紋章（56x56）
build.mjs               結合・検証・dist生成・アイコン生成
icongen.mjs             ドット絵からPNG/ICO/SVGを作る（外部依存なし）
```

ファイル1つが章（ステージ）1つに対応する。章データは別に持たず `build.mjs` の
`CHAP_ORDER` がファイル構成から起こす。**章を増やすときはデータファイルを足して
`CHAP_ORDER` に1行追加するだけ**でよい。

## N5版で絶対に崩してはいけない前提

**学習者は漢字が読めない。** N2版との最大の違いはここ。

- 画面に出る日本語は「ルビ付きの漢字」か「かな」のどちらかしか許さない
- データ側は `[漢字|よみ]` と書く。テンプレートの `rb()` が `<ruby>` に開く
- **`build.mjs` の `checkRuby()` が全データを検査し、ふりがなの無い漢字があればビルドを止める**
- 例外は見出し語（語・熟語・文型）だけ。これらは読みを別フィールドに持つ

**「日本語が完全に初めて」の人も対象。** かな139字がステージ1〜2にあり、
ローマ字表示（`S.romaji`）を実行時変換で出せる。ローマ字はデータに持たない。

UIはひらがな主体。英語のグロス（`gl()`）を全面に添え、`S.uiEn` で消せる。
UI文言に漢字を足すときは、かなにするか、iOSのメニュー名のように
「実物と一致させる必要がある文字列」かを考えること。

## データのスキーマ

```
kana    [かな, ローマ字, 例語, 例語のローマ字, 英語]
vocab   [語, 読み, 品詞, 意味, 英語, 例文]
kanji   [漢字, 音, 訓, 字義, [[例語, 読み, 意味, 英語], ...]]
grammar [文型, 接続, 意味, 英語, 例文, 注意]
```

例文の出題対象は `【…】` で囲む（穴埋めの空欄になる）。1〜2組まで。
意味・接続・注意は原則ひらがなで書く。漢字を使うならルビを付ける。

## build.mjs が保証していること

壊したらビルドが落ちる。**落ちたら直す。検証を緩めない。**

- ふりがなの無い漢字がない（`checkRuby`）
- かな139字のローマ字が `romaji.js` の変換結果と一致する
- 漢字が、もらった一覧表（11行103字）と過不足なく一致する（`N5_KANJI_LIST`）
- 例文に `【】` が1〜2組ある
- 全項目がどこかの章に1回だけ入っている
- 章ボスが全章ちがう
- ドット絵が正方形で、パレットに未定義・未使用の色がない
- 出力した `data.js` / `app.js` が構文として通る

最後の1つは、テンプレートの文字列に生の改行が紛れてブラウザで真っ白になる事故を
止めるためのもの。**テンプレートを機械的に書き換えるときは `\n` の扱いに注意。**

## 出題とSRS

`ITEMS` は かな/語彙/漢字/文法 の4セクション。IDは `n:` `v:` `k:` `g:` の接頭辞。
漢字は熟語ではなく**1字**が1項目（熟語を項目にすると語彙側と二重登録になる）。

SRSは `S.srs[id] = {lv, due, seen, miss}`。lv0..6、`INTERVALS` の日数で再出題。
**初出（lv<=1）はいちばん素直な出題形式に固定**し、慣れてから他の形式を混ぜる。

新出項目は「いまの章」のものを優先して出す（`buildQueue`）。

## 保存

`localStorage` の `n5quest:s1..s3`（ぼうけんのしょ3つ）。アカウントなし、端末間同期なし。
`window.claude.use('db')` の経路はClaude Artifact内でのみ有効で、Vercel版では動かない。

- 読み書きは必ず `lsGet` / `lsSet` を通す（localStorage自体が例外を投げる環境がある）
- `rollDay()` の変更は `persist()` ではなく `writeSlot()` で保存する。
  開いただけで `updatedAt` が進むと、別端末の新しい書をクラウドから拾えなくなる
- iOS Safariは7日さわらないと保存領域を消す。ホーム画面アプリなら対象外なので、
  iOSかつ非standaloneのときだけ案内を出している

## 著作権

語義・例文・設問はすべてこのアプリのための書き下ろし。
市販教材の本文は一切使っていない。**この方針は変えない。**
出題範囲の一覧（漢字表・語彙リスト）は参考にしてよいが、本文は写さない。
